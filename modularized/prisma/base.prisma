generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// <include path="./schema/role.prisma" />
/// <include path="./schema/user.prisma" />